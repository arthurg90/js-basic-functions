/*Boiler Plate Example function

let add = (a, b) => a + b;

let result = add(1, 2);
console.log(result)

*/

//Function to concatenate 2 strings

let concat = (a, b) => a + " " + b;     //this line defines the function and take strings
let result = concat("fish", "sticks");  //this is where the arguements of the two strings get placed (can be whatever two strings)
console.log(result)

