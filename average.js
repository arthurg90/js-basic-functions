/*
Create a function, average, that takes three numbers as arguments. The function should return the average of the numbers.

*/



let average = (x, y, z) => {
	return (x + y + z) / 3;		
}

let output = average(1, 2, 3);
console.log(output);